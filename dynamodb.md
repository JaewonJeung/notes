# dynamodb.md

PartionKey : (Optional) Sort Key

If sort key exists, it becomes a composite key.

So things like partition key being the chat room and the monotonic message id being the sort key is possible 

DAX
- in mem cache like redis

Stream
- similar to [[Change data capture (CDC)]]