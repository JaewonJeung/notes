# Change data capture (CDC)

CDC is a way of capturing all the changes occurring in your database and doing something with it.

- highly efficient for extracting data changes without impacting the source system significantly.
- Can be used for near-real-time data sync with [[elasticsearch]]